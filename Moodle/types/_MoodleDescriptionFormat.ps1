# This file is prefixed with an underscore so it is dot-sourced earlier than 
# other files, because the enum is shared between multiple classes.
enum MoodleDescriptionFormat {
    Moodle = 0;
    HTML = 1;
    Plain = 2;
    Markdown = 4;
}